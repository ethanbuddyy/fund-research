# 版本下限与 requirements.lock / 实际运行环境对齐，并加主版本上限，
# 避免 `pip install -r requirements.txt` 解析到与 lock 跨主版本的不兼容版本
# （历史上 yfinance 旧版 `show_errors` 兼容 bug 即源于下限过低）。
# 需要可复现安装时请优先使用：pip install -r requirements.lock
yfinance>=1.4,<2
akshare>=1.18,<2
fredapi>=0.5.2,<1
pandas>=3.0,<4
numpy>=2.0,<3
scipy>=1.11,<2
requests>=2.31,<3
PyYAML>=6.0,<7
schedule>=1.2,<2
python-dateutil>=2.8.2,<3
xlrd>=2.0,<3             # Shiller CAPE 历史 .xls 文件解析
anthropic>=0.105,<1
openai>=2.0,<3          # DeepSeek / OpenAI backend（可选）
json-repair>=0.30,<1    # DeepSeek 返回非法 JSON 时自动修复（可选）
